# Deepseek-harness-for-seniors：面向中老年人的本地 AI 任务工作台设计方案(Deepseek版本)

**文档版本**：v1.0
**日期**：2026-09-22
**适用范围**：Windows `.exe` 与 macOS `.dmg` 桌面应用，首期接入 DeepSeek

## 1. 项目概述

### 1.1 要解决的问题

中老年用户有整理文件、生成材料、处理表格和撰写文档等复杂需求，但通常遇到三个障碍：

1. 不知道如何把想法写成准确的 Prompt。
2. 不熟悉命令行、沙箱、文件路径和权限授权。
3. 即使 AI 完成了操作，也不容易确认结果在哪里、结果是否可靠，以及如何继续修改。

### 1.2 产品定位

AI for the old 是一个“说清楚需求，AI 在电脑上完成任务”的桌面工作台。用户只需要用日常语言描述目标，应用负责：

- 用最多三个候选选项帮助用户确认真实意图；
- 在必要时进行多轮、一次一个问题的澄清；
- 在用户一次性同意的范围内搜索本机文件并建立任务 workspace；
- 调用 DeepSeek 和本地工具完成任务；
- 把结果放入清晰、固定的位置，并用人话说明结果；
- 在当前任务页收集反馈，自动进入下一轮修改。

### 1.3 设计原则

- **少选择、强引导**：每一步只让用户做一个明确决定。
- **大字、低密度、可恢复**：字号不低于 16px，关键操作有撤销、重试和恢复入口。
- **本地优先**：文件默认留在本机；上传云端前明确展示范围和用途。
- **先同意、后执行**：在任务开始前完成一次权限和操作范围确认，执行中不反复打断。
- **不展示思维链**：用户只看到“正在处理”、阶段进度和计时器，不显示模型内部思考。
- **结果可追溯**：每次任务都能看到输入文件、生成文件、执行记录和反馈历史。
- 能复用 https://github.com/deepseek-ai/deepseek-harness(本地clone /Users/jiaweiguo/Deepseek-harness-for-seniors/deepseek-harness) 的界面和代码功能，尽量复用，风格尽量与原版 harness 统一。

## 2. 用户与典型任务

### 2.1 目标用户

- 需要处理家庭、社区、退休后兼职或小型经营事务的中老年用户；
- 计算机能力有限，但会使用微信、扫码和基本文件操作；
- 需要家人或工作人员远程协助的用户。

### 2.2 典型场景

| 场景 | 用户说法 | 期望结果 |
| --- | --- | --- |
| 文件整理 | “把桌面上孩子发来的照片按年份整理好” | 按日期/主题分文件夹并生成整理说明 |
| 材料撰写 | “根据这些材料帮我写一个社区活动通知” | 生成可编辑的 Word/PDF |
| 表格处理 | “把下载文件夹里的账单合成一个月度表” | 生成汇总表、计算结果和异常说明 |
| 文件查找 | “找出去年买药的发票” | 返回候选文件、所在位置和匹配理由 |
| 结果修改 | “通知写得太正式，改得亲切一点” | 在原任务页继续修改并保留版本 |

## 3. 产品形态与总体架构

### 3.1 桌面端形态

首期采用 **Tauri 2 + React + TypeScript**：

- Tauri 负责 Windows/macOS 安装包、原生文件系统和系统权限；
- React 负责可访问的交互界面；
- Rust 原生层负责文件索引、权限检查、进程隔离和本地任务执行；
- 任务执行器采用本地 sidecar（可用 Python 或 Rust 实现），避免把用户文件直接交给浏览器层；
- 安装包只提供一个“安装 AI for the old”主操作，不提供高级安装选项。

### 3.2 系统分层

```text
桌面应用（Tauri）
├─ React UI：登录、任务、结果、反馈、设置
├─ Native Bridge：系统权限、文件选择、通知、托盘
├─ Local Agent Runtime：文件索引、workspace、工具执行、结果导出
└─ Secure Credential Store：系统钥匙串/凭据管理器
          │ HTTPS / WebSocket
云端控制面（服务端）
├─ API Gateway：认证、限流、版本兼容
├─ Task Orchestrator：澄清、规划、执行、反馈状态机
├─ LLM Provider Adapter：DeepSeek 适配层
├─ Billing Service：账户余额、支付二维码、订单回调
├─ Artifact/Sync Service：任务元数据、可选的云端结果同步
└─ Observability：指标、错误追踪、脱敏审计日志
```

### 3.3 云端与本地的边界

注意，本应用要求不需要一个除了deepseek官方网站之外的服务器。所有的其它操作可以通过设计prompt, plugin, 调用deepseek官方API的方式完成。

| 能力 | 默认位置 | 说明 |
| --- | --- | --- |
| Prompt 澄清与任务规划 | 云端 | 发送经过脱敏/用户同意的任务描述；支持 DeepSeek 更换 |
| 文件搜索、复制、重命名、转换 | 本地 | 原始文件默认不上传 |
| 需要理解文件内容的模型调用 | 云端或本地可配置 | 上传前显示文件清单、类型和大小； |
| workspace 与生成文件 | 本地 | 默认位于桌面 `AI for the old/任务名称-日期/` |
| 账户、用量、充值订单 | 云端 | 余额和订单不保存在客户端明文 |
| 任务历史索引 | 本地 + 云端摘要 | 本地保存完整记录；云端只保存必要元数据，用户可关闭同步 |

## 4. 核心用户流程

### 4.1 首次启动与登录

1. 欢迎页只展示一句话：“把要做的事告诉我，我来帮你完成。”
2. 点击“扫码登录”，显示二维码和大字号的手机操作提示。
3. 手机确认后，桌面端自动进入账户初始化。
4. 首次使用引导用户选择：默认 workspace 位置、是否允许读取常用文件夹、是否允许联网调用 AI。
5. 用一个总开关展示授权状态，授权完成后才进入任务首页。

登录设计要求：

- 直接使用 https://platform.deepseek.com/sign_in 界面的二维码或者手机号登录，默认就是 deepseek 的 API。

### 4.2 充值与用量

首页显示“账户余额/本月用量”，点击后进入充值页：

- 展示 3 个推荐金额和“自定义金额”；
- 后端创建订单并返回支付宝/微信支付二维码；
- 用户用手机扫码支付，桌面端轮询或通过 WebSocket 接收支付结果；
- 支付成功后展示到账金额、订单号和余额变化；
- 充值失败、订单过期、重复支付均提供明确处理路径。

支付由服务端对接 DeepSeek 官方 API 充值渠道，客户端不得保存支付密钥，也不得根据客户端回传结果直接加余额。

### 4.3 新建任务与 Prompt 自动细化

首页核心区域是一个大输入框，提示语为：“你想让电脑帮你做什么？”

流程：

1. 用户输入初始描述并点击“开始整理”。
2. 服务端返回最多 3 个候选意图卡片，每张卡片包含：简短标题、我理解的目标、可能需要的文件。
3. 用户可以选择一个候选项，或点击“都不是，我补充说明”。
4. 若仍不明确，系统每轮只问一个问题；优先使用单选，其次使用大输入框。
5. 每轮都显示“已确认内容”，允许用户点击编辑，不要求重新输入全部内容。
6. 当任务足够明确时显示执行预览：目标、将访问的位置、预计产物、预计耗时和可能的联网调用。
7. 用户点击“确认并开始”，进入执行阶段。

澄清策略：

- 优先确认目标产物（例如 Word、PDF、文件夹或说明）；
- 再确认范围（时间、文件夹、对象）；
- 最后确认偏好（格式、语气、命名）；
- 连续两轮无法形成可执行计划时，提供“联系家人/人工协助”入口。

### 4.4 权限与文件搜集

执行预览页一次性列出权限：

- 读取：桌面、下载、微信文件等常用位置；
- 写入：默认 workspace 目录；
- 联网：调用 DeepSeek、同步任务状态和支付；
- 可选：读取剪贴板、发送系统通知。

用户不需要勾选，直接点击“允许本次任务使用”即可，并且要提示用户如何处理之后的系统级授权。应用随后完成系统级授权检查。Windows/macOS 的实际系统弹窗只能由操作系统显示，应用不绕过系统授权。

本地 Agent 的文件搜集流程：

1. 根据任务范围生成目录白名单；
2. 扫描文件名、扩展名、时间、大小和可读元数据；
3. 仅在必要时读取内容，并记录读取原因；
4. 在固定 workspace 创建 `input/`、`work/`、`output/`、`logs/`；
5. 原文件采用复制或只读链接方式，不直接修改原文件；
6. 任务完成后生成 `任务说明.md`，写明使用过的文件和结果路径。

### 4.5 执行中的界面

执行页保持稳定，不显示思考过程，只显示：

- 状态：正在查找文件 / 正在整理 / 正在生成 / 正在检查结果；
- 当前阶段进度条（阶段型，不承诺精确百分比）；
- 已用时间和预计剩余时间区间；
- 已找到文件数、已完成步骤数、输出文件数；
- “暂停任务”“停止任务”“查看任务详情”三个操作。

预计时间由历史任务、文件数和工具步骤估算。若无法估算，显示“正在估算所需时间”，避免伪造精确时间。

### 4.6 完成、结果与反馈闭环

完成页首先显示结论：“已经完成，结果在桌面上的 XXX 文件夹。”

结果区域包含：

- “打开结果文件夹”；
- 主要产物列表，显示文件类型、大小和生成时间；
- “预览”“打开”“复制路径”；
- 使用过的输入文件摘要；
- 任务说明、异常、未处理项和建议人工检查的内容。

同一任务页底部固定显示反馈选项：

1. **没有问题，完成得很好**；
2. 文件找错了；
3. 内容不准确；
4. 格式或排版不合适；
5. 漏掉了一些内容；
6. 我想换一种做法；
7. 其他问题（补充说明）。

选择非第一项后，系统把反馈自动转成下一轮澄清上下文，先展示“我会这样修改”，用户确认后重新执行。每轮生成新版本，不覆盖旧版本；用户可以比较和恢复到任意版本。

## 5. 前端交互设计

### 5.1 信息架构

- **首页**：新建任务、最近任务、账户余额、帮助入口。
- **任务页**：澄清、执行、结果、反馈，按时间轴保留在一个页面。
- **任务历史**：按进行中/已完成/需要处理筛选，可搜索任务名。
- **账户与充值**：余额、用量、充值、订单。
- **设置**：workspace、文件权限、联网与隐私、通知、家人协助、关于。

### 5.2 主要组件

- `TaskComposer`：大输入框、录音按钮（后续版本）、开始按钮。
- `IntentOptions`：最多 3 个候选意图卡片，支持键盘和点击。
- `ClarificationStep`：单问题、多种输入类型、已确认内容。
- `AccessScopePanel`：文件夹、联网和写入范围的权限清单。
- `TaskProgress`：状态、阶段进度、计时器、可暂停/停止。
- `ArtifactList`：结果文件、预览、打开、复制路径。
- `FeedbackOptions`：固定反馈选项与自由补充。
- `TaskTimeline`：用户输入、澄清、执行、结果和反馈的版本时间轴。

### 5.3 可访问性与容错

- 默认字号 18px，标题 28px 以上；支持一键放大到 125%/150%。
- 高对比度主题，颜色不作为唯一状态信号；成功、警告、失败同时显示图标和文字。
- 所有按钮使用动词，如“打开结果文件夹”“继续修改”；避免只用图标。
- 重要操作提供 5 秒撤销提示；停止任务后保留已生成的中间结果。
- 网络断开时明确显示“网络已断开”，已下载/已生成的本地结果仍可打开。
- 错误提示包含“发生了什么”“我已经做了什么”“你可以怎么办”三部分。
- 支持键盘操作、屏幕阅读器标签和 Windows 高对比度模式。

### 5.4 状态机

```text
NEW
  -> CLARIFYING       初始描述需要确认
  -> READY_TO_RUN     目标、范围、输出已明确
  -> ACCESS_PENDING   等待一次性权限确认
  -> RUNNING          本地搜集与 Agent 执行
  -> PAUSED           用户主动暂停或等待网络
  -> REVIEW_REQUIRED  结果生成，但存在异常/需人工检查
  -> COMPLETED        结果可交付
  -> FEEDBACK_PENDING 等待用户反馈
  -> REVISION         基于反馈重新澄清和执行
  -> FAILED           可重试或人工协助
  -> CANCELLED        用户停止，保留已产生文件
```

## 6. 后端设计

### 6.1 服务模块

#### API Gateway

- 统一认证、设备绑定、限流和 API 版本；
- 为桌面端提供 HTTPS REST API 和 WebSocket 任务事件；
- 对客户端版本和 sidecar 版本做兼容检查。

#### Auth Service

- DeepSeek 二维码登录、手机号备用登录、设备会话和 token 刷新；
- 风险控制：二维码过期、异地设备、频繁登录；
- 账户删除和数据导出。

#### Clarification Service

- 接收用户原始描述和上下文；
- 调用 DeepSeek 生成候选意图、下一问题和执行预览；
- 输出严格 JSON Schema，禁止直接输出可执行 shell 命令；
- 保存确认字段，保证多轮对话可恢复。

#### Task Orchestrator

- 按状态机驱动任务；
- 把模型计划转换为受限工具调用；
- 管理重试、超时、暂停、恢复、取消和幂等；
- 通过 WebSocket 推送阶段进度和结果事件。

#### DeepSeek Provider Adapter

- 统一封装模型、超时、重试、用量统计和错误映射；
- API key 只在服务端保存；
- 可配置模型和温度，后续可增加其他 provider；
- 对上传文件做类型、大小、敏感信息策略检查。

#### Billing Service

- 账户余额、价格规则、用量扣费、充值订单和退款；
- 支付回调必须验签并幂等处理；
- 用量不足时任务进入 `WAITING_FOR_TOPUP`，不丢失上下文。

#### Local Agent Control Plane

- 下发签名后的任务计划和能力范围；
- 接收本地 Agent 的心跳、阶段、日志摘要和产物清单；
- 不接受客户端任意上传的命令；所有工具调用必须匹配 allowlist。

### 6.2 本地 Agent 安全模型

Agent 的执行计划使用结构化工具，不直接执行模型生成的命令：

```json
{
  "tool": "copy_files",
  "source_scope": "desktop/微信文件",
  "selectors": {"extensions": [".jpg", ".png"], "created_after": "2025-01-01"},
  "destination": "workspace/input/photos",
  "mode": "copy"
}
```

首期工具 allowlist：`list_files`、`read_metadata`、`read_text`、`copy_files`、`move_to_trash`、`create_directory`、`write_text`、`convert_document`、`create_spreadsheet`、`open_result`。危险操作默认采用复制、版本化或系统回收站，禁止模型直接删除原文件、修改系统目录或运行任意网络下载。

### 6.3 数据存储

- **PostgreSQL**：账户、设备、任务元数据、澄清字段、订单、用量、反馈。
- **对象存储（可选）**：用户明确开启同步后存放加密产物；默认不存原文件。
- **Redis**：二维码会话、短期任务状态、限流和 WebSocket 会话。
- **本地 SQLite**：任务完整事件、文件索引、版本和离线队列。
- **系统钥匙串**：登录 token、设备密钥和本地 Agent 配对密钥。

## 7. 数据模型

核心表建议如下：

| 表 | 关键字段 | 用途 |
| --- | --- | --- |
| `users` | `id`, `phone_hash`, `created_at`, `status` | 用户账户 |
| `devices` | `id`, `user_id`, `platform`, `last_seen`, `revoked_at` | 设备会话 |
| `tasks` | `id`, `user_id`, `title`, `status`, `workspace_path`, `created_at` | 任务主记录 |
| `task_turns` | `id`, `task_id`, `role`, `content`, `structured_data`, `created_at` | 澄清与反馈轮次 |
| `task_plans` | `id`, `task_id`, `plan_json`, `scope_json`, `version` | 执行计划版本 |
| `task_events` | `id`, `task_id`, `event_type`, `payload`, `occurred_at` | 可恢复事件流 |
| `artifacts` | `id`, `task_id`, `path`, `kind`, `checksum`, `version` | 输出文件清单 |
| `feedback` | `id`, `task_id`, `category`, `comment`, `created_at` | 结果反馈 |
| `wallets` | `user_id`, `balance`, `updated_at` | 账户余额 |
| `orders` | `id`, `user_id`, `amount`, `status`, `provider_trade_id` | 充值订单 |
| `usage_records` | `id`, `user_id`, `task_id`, `tokens`, `amount`, `created_at` | 用量与扣费 |

## 8. API 草案

所有接口使用 `/api/v1` 前缀，错误返回统一格式：`{ code, message, retryable, request_id }`。

### 8.1 登录

- `POST /auth/qr-sessions`：创建二维码会话，返回 `session_id`、二维码内容和过期时间。
- `GET /auth/qr-sessions/{id}`：查询 `pending/success/expired`。
- `POST /auth/token/refresh`：刷新设备 token。

### 8.2 澄清与任务

- `POST /tasks`：创建任务，提交原始描述。
- `POST /tasks/{id}/clarifications`：提交候选项或补充说明，返回下一步或 `ready_to_run`。
- `POST /tasks/{id}/access-scope`：提交本次任务的访问范围和用户同意记录。
- `POST /tasks/{id}/run`：幂等启动任务。
- `POST /tasks/{id}/pause`、`/resume`、`/cancel`：控制执行。
- `GET /tasks/{id}`：读取任务、阶段、产物和异常。
- `GET /tasks/{id}/events`：断线后按游标恢复事件。
- `POST /tasks/{id}/feedback`：提交反馈并创建修订轮次。

### 8.3 充值

- `GET /billing/balance`：查询余额和用量。
- `POST /billing/orders`：创建充值订单，返回二维码和过期时间。
- `GET /billing/orders/{id}`：查询订单状态。
- `POST /billing/provider-callback`：支付渠道回调，服务端验签并幂等入账。

### 8.4 WebSocket 事件

客户端订阅 `/ws/tasks/{task_id}`，事件示例：

```json
{
  "event": "task.progress",
  "task_id": "task_123",
  "stage": "generating",
  "label": "正在生成文档",
  "completed_steps": 4,
  "total_steps": 6,
  "elapsed_seconds": 38,
  "eta_seconds": 42
}
```

## 9. 安全、隐私与合规

- 首次授权明确列出读取、写入、联网范围；任务中不得偷偷扩大范围。
- 文件默认不上传；需要上传时显示文件名、大小、原因和目标服务，并支持取消。
- 应用没有云端，数据纯本地，基于需要调用 DeepSeek 的相关 auth 和 API。
- 本地 workspace 设置 ACL/权限隔离，任务结束可一键清理临时 `work/` 和 `logs/`。
- 任务计划和 Agent 包使用签名校验；自动更新支持回滚。
- 对高风险操作（覆盖原文件、批量移动、敏感文件上传）在执行预览阶段单独标红并要求明确加入授权范围。
- 提供隐私中心：查看、导出、删除任务数据，撤销文件夹授权，注销账户。
- 失败恢复：本地日志采用追加写入；任务可从最近一个已确认事件继续，不重复扣费或重复生成。

## 10. 可观测性与质量指标

### 10.1 关键指标

- 首次登录成功率、二维码登录平均耗时；
- 从初始描述到 `READY_TO_RUN` 的平均轮数；
- 任务完成率、失败率、平均耗时和重试率；
- 结果打开率、反馈率、反馈后二次完成率；
- 文件误选率、原文件误修改率（目标为 0）；
- 充值成功率、支付回调延迟和重复入账数（目标为 0）。

### 10.2 测试策略

- 单元测试：澄清 JSON Schema、状态机、扣费幂等、文件范围匹配。
- 集成测试：二维码登录、支付回调、WebSocket 断线恢复、本地 Agent 通讯。
- 安全测试：路径穿越、命令注入、越权访问、恶意文档、权限扩大。
- 端到端测试：Windows/macOS 安装、首次授权、照片整理、Word 生成、反馈修改。
- 可用性测试：邀请 5-8 名目标用户完成“整理照片”和“生成通知”两个任务，观察是否需要家人介入。

## 11. MVP

- Windows/macOS 安装包和首次引导；
- 扫码登录、余额展示、测试环境充值；
- DeepSeek 澄清流程（候选项 + 多轮单问题）；
- 桌面/下载/用户选择目录的文件索引；
- 本地 workspace、基础文件复制与文档生成；
- 任务进度、结果文件夹、任务内反馈和版本化重跑；
- SQLite 离线事件与失败恢复。

## 12. MVP 验收标准

1. 用户只通过一次安装即可启动应用，不需要手动安装 Python、Node 或命令行工具。
2. 新用户能在 3 分钟内完成扫码登录并进入首页，且自动联通 DeepSeek 的可用 API，默认使用最新模型。
3. 对模糊描述，系统能给出不超过 3 个可理解选项，并能通过至少 3 轮澄清形成可执行计划。
4. 用户确认权限后，任务执行过程中不重复弹出同一权限询问。
5. 任务默认只在 workspace 操作，不修改原始文件；停止或失败后仍能找到已生成文件。
6. 执行页不显示模型思考内容，只显示可理解的阶段、计时和进度估计。
7. 完成页能明确告诉用户结果文件夹和每个主要文件的路径。
8. 用户选择“内容不准确”等反馈后，系统能在同一任务页启动下一轮，并保留上一版本结果。
9. 网络中断、应用重启后，任务状态和结果不会丢失；支付回调不会重复入账。
