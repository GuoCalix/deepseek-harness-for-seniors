# AI for the old

解决问题：中老年人往往有处理文件等需要client agent的复杂需求，但是由于没有精确表达需求的prompt能力和不熟悉agent的命令行和沙箱等操作，导致无法正确使用AI，也没有办法有效利用AI给出的结果。

解决方案，制作一个专门针对中老年人的AI harness。要求如下：

- 自动接入deepseek，登录操作简单，直接扫码登录，充值也简单可以在手机上扫码后操作。
- 只有一个传统安装包（.exe或者.dmg)，安装后拆箱即用，点一个安装就可以安装，没有复杂选项需要勾选。
- 自动细化prompt，当用户输入初始prompt，通过回复3个可能选项让用户选择自己想表达的意思，或者detail另外的意思。如果prompt仍然不明确，可以使用多轮的确认，来逐步细化prompt。
- 要求用户给予所有文件的访问和操作权限，可以自己在电脑上搜集到所需的文件，（比如从桌面，下载，微信文件等），然后自己在一个固定的目录（默认桌面）创建一个workspace沙箱，再执行操作。操作完成告诉用户结果在哪个文件夹的哪个文件中。
- 完成task之后显示一组反馈选项，写出可能的有问题有瑕疵的地方，做成一组选项，并且保持在那个task的选项卡中，第一个选项是没有问题，如果用户选择了其他选项，则自动进入下一轮的prompt细化和task流程。
- task流程中默认不显示思考，也不需要用户授权，只显示一个思考中和计时器，以及一个估计所需时间的进度条。
